package _03_ifElseKosulYapisi;

public class ifElse {
    public static void main(String[] args) {

        /*
         if ingilizcede eğer anlamına gelmektedir. Aynı şekilde programlama
         dillerinde de eğer anlamına gelmektedir. Genel yapısı aşağıdaki gibidir.
         */

        /*
        if (boolean) //true veya false
        true ise yapılacak işlemler
        else
        false ise yapılacak işlemler
         */


        /*
        Bir java uygulaması yazdığımızı farz edelim.
        (İf koşulunun mantığı tüm programlama dillerinde aynıdır,
        sadece syntax farklı olabilir). Bu uygulama içinde bir kod bloğunun
        çalışması için bir şart gerekli diyelim. Örneğin çarpma işlemi yapacağız
         klavyeden girilen bir sayıyı 5 ile çarpacağız, ama girilen sayının
         0’dan büyük olmasını istiyoruz. Bu ve buna benzer durumlarda kullanılan
         if koşulunun kullanımına geçmeden önce if koşulunu oluştururken
         sıkça kullanacağımız karşılaştırma operatörlerinden bahsedeceğim.
         */

        /*
     Operatör	Açıklama/Örnek Kullanım
 == (eşit)	İki değer eşitse true, değilse false
(A==B) => false

 != (eşit değil)	İki değer eşit değilse true, eşitse false
(A!=B) => true

 > (büyüktür)	ilk değer ikincisinden büyükse true,değilse false
(A>B) => false

 < (küçüktür)	ilk değer ikincisinden küçükse true, değilse false
(A<B) => true

 >= (büyük eşitse)	ilk değer ikincisinden büyük veya eşitse true,değilse false
(A>B) => false

 <= (küçük eşitse)	ilk değer ikincisinden küçük veya eşitse true,değilse false
(A<B) => true
         */

        /*
        Yukarıdaki operatörleri if koşullarını oluştururken kullanacağız.
        Bunlara ek olarak string karşılaştırmaları yapacağımız
        .equals methodu bulunmaktadır.
         */
        
        String x = "IfElse";
        String y = "Zeliha Oznuk";
        
        if (x.equals(y)){ // //return false çünkü farklı stringler

        }



        









    }
}
